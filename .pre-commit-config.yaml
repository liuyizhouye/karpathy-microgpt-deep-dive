repos:
  - repo: local
    hooks:
      - id: strip-notebook-outputs
        name: Strip Jupyter notebook outputs
        entry: python3 scripts/strip_notebook_outputs.py --files
        language: system
        files: \.ipynb$
        pass_filenames: true
