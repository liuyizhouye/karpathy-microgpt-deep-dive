name: Notebook Cleanliness

on:
  push:
  pull_request:

jobs:
  notebook-cleanliness:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check notebooks have no saved outputs
        run: python3 scripts/strip_notebook_outputs.py --all --check
